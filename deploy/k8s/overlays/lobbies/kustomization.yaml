apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pichess
# lobbies = MVP + lobby-service (still in-memory, still no datastore).
resources:
  - ../mvp
  - lobby-service.yaml

patches:
  - path: patch-gateway-lobby.yaml

images:
  - name: ghcr.io/jonaskromer/pichess-lobby-service
    newTag: "0.3.1"
