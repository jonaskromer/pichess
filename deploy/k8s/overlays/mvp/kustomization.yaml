apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pichess
# MVP = the base as-is (gateway + game-service, in-memory). The `lobbies` and
# `full` overlays will layer onto this same base.
resources:
  - ../../base
